section data vars at $1000
  counter: word = 0
end

func read_counter(): HL
  move hl, counter
end

func write_counter(new_value: word)
  move hl, new_value
  move counter, hl
end

export func main()
  var
    local_alias = counter
  end

  write_counter 123
  move hl, local_alias
end
