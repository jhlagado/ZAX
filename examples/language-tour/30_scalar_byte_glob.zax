section code at $0100
section data at $2000

data
  glob_b: byte = $AA
end

export func touch_scalar_byte_glob(): HL
  ld a, glob_b
  ld glob_b, a
  ld h, 0
  ld l, a
end

export func main()
  touch_scalar_byte_glob
end
