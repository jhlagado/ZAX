data
  glob_bytes: byte[8] = { 0,1,2,3,4,5,6,7 }

func byte_glob_reg(idx: byte): HL
  ld b, glob_bytes[idx]
  ld glob_bytes[idx], b
  ld h, 0
  ld l, b
end

export func main()
  byte_glob_reg $0003
end
