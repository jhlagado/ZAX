section code at $0100
section data at $2000

data
  glob_bytes: byte[8] = { 0,1,2,3,4,5,6,7 }
  glob_idx_word: word = 4
end

export func byte_glob_glob(): HL
  ld a, glob_bytes[glob_idx_word]
  ld glob_bytes[glob_idx_word], a
  ld h, 0
  ld l, a
end

export func main()
  byte_glob_glob
end
